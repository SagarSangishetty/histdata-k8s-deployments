{{- if .Values.externalSecret.enabled }}
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: {{ .Values.externalSecret.secretStoreName }}
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.application.awsRegion }}
{{- end }}

