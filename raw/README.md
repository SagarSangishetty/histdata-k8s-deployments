# Raw manifests

These files are intentionally explicit for the first manual Kubernetes phase.
Replace every `REPLACE` value, create the database secret safely, then apply in
numeric order. Never commit a real Secret file.

After learning the objects, remove this release and use the Helm chart for
repeatable deployments.

